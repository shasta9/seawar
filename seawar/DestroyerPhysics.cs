namespace seawar {
   public class DestroyerPhysics : IPhysics {
      public bool TryMoveTo(Tile pos) {
         // port?
         // not water?
         // aground
         // for each actor on the tile
         // merchant ship?
         //  collision
         // submarine?
         // surfaced?
         // resolve collision
         // periscope depth?
         // collision
         // submerged?
         // collision
         // torpedo?
         // mine?
         return true;
      }
   }
}
